
import { tss } from '../../tss/tss'
import name from '../names/html';

export default tss(
    {
        E: name,
        line_height: `1.15`,
        _webkit_text_size_adjust: `100%`,
    },
    {
        E: `[hidden]`,
        display: `none`
    },
);
