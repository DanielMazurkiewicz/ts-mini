
import { tss } from '../../tss/tss'
import name from '../names/legend';

export default tss({
    E: name,
    box_sizing: 'border-box',
    color: 'inherit',
    display: 'table',
    max_width:' 100%',
    padding: '0',
    white_space: 'normal',
});
