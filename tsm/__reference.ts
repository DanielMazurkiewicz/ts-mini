// import { decorator } from '../DOM/base'
// import { tss, layout, place } from '../tss'

// export const hoverSelector = `=:hover`
// export const paritySelector = `=:nth-of-type(2n+1)`




// export const prepareComponent = (style: string) => (...children: any) => {
//     const element = document.createElement('div');
//     element.classList.add(style)
//     return decorator(element, children);
// }



// export const text = (data: string) => document.createTextNode(data);

// export const hcenter = prepareComponent(contentStyle);
// export const vcenter = prepareComponent(contentStyle);
